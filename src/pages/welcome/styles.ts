import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 16,
        justifyContent: 'flex-end',
        alignItems: 'center',
        gap: 16,
    },

    title: {
        fontSize: 40,
        fontWeight: 'bold',
        color: '#FFF',
    },

    text: {
        color: '#FFF',
        fontSize: 20,
    }
})