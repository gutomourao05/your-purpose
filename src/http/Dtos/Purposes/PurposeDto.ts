export type PurposeDto = {
    id: string;
    name: string;
    startDate: string;
    endDate: string;
    withAlert: boolean;
    isActive: boolean
    updatedAt: string
    notifications: string[];
};
